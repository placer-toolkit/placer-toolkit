---
title: Tooltip
description: Tooltips display additional information based on a specific action.
category: feedbackAndStatus
---

Tooltips display additional information based on a specific action.

A tooltip’s target is its _first child element_, so you should only wrap one element inside the tooltip. If you need the tooltip to show up for multiple elements, nest them inside a container first.

```demo
<pc-tooltip content="This is a tooltip!">
    <pc-button>Hover me</pc-button>
</pc-tooltip>
```

## Demos

### Placement

Use the `placement` attribute to set the preferred placement of the tooltip.

```demo
<div class="tooltip-placement">
    <pc-tooltip content="top-start" placement="top-start" style="grid-area: top-start">
        <pc-button>
            <pc-icon
                class="pc-visually-hidden"
                library="default"
                icon-style="solid"
                name="arrow-up"
                label="Top start"
                style="rotate: -45deg"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="top" placement="top" style="grid-area: top">
        <pc-button>
            <pc-icon
                library="default"
                icon-style="solid"
                name="arrow-up"
                label="Top"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="top-end" placement="top-end" style="grid-area: top-end">
        <pc-button>
            <pc-icon
                class="pc-visually-hidden"
                library="default"
                icon-style="solid"
                name="arrow-up"
                label="Top end"
                style="rotate: 45deg"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="left-start" placement="left-start" style="grid-area: left-start">
        <pc-button>
            <pc-icon
                class="pc-visually-hidden"
                library="default"
                icon-style="solid"
                name="arrow-left"
                label="Left start"
                style="rotate: 45deg"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="left" placement="left" style="grid-area: left">
        <pc-button>
            <pc-icon
                library="default"
                icon-style="solid"
                name="arrow-left"
                label="Left"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="left-end" placement="left-end" style="grid-area: left-end">
        <pc-button>
            <pc-icon
                class="pc-visually-hidden"
                library="default"
                icon-style="solid"
                name="arrow-left"
                label="Left end"
                style="rotate: -45deg"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="right-start" placement="right-start" style="grid-area: right-start">
        <pc-button>
            <pc-icon
                class="pc-visually-hidden"
                library="default"
                icon-style="solid"
                name="arrow-right"
                label="Right start"
                style="rotate: -45deg"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="right" placement="right" style="grid-area: right">
        <pc-button>
            <pc-icon
                library="default"
                icon-style="solid"
                name="arrow-right"
                label="Right"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="right-end" placement="right-end" style="grid-area: right-end">
        <pc-button>
            <pc-icon
                class="pc-visually-hidden"
                library="default"
                icon-style="solid"
                name="arrow-right"
                label="Right end"
                style="rotate: 45deg"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="bottom-start" placement="bottom-start" style="grid-area: bottom-start">
        <pc-button>
            <pc-icon
                class="pc-visually-hidden"
                library="default"
                icon-style="solid"
                name="arrow-down"
                label="Bottom start"
                style="rotate: 45deg"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="bottom" placement="bottom" style="grid-area: bottom">
        <pc-button>
            <pc-icon
                library="default"
                icon-style="solid"
                name="arrow-down"
                label="Bottom"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>

    <pc-tooltip content="bottom-end" placement="bottom-end" style="grid-area: bottom-end">
        <pc-button>
            <pc-icon
                class="pc-visually-hidden"
                library="default"
                icon-style="solid"
                name="arrow-down"
                label="Bottom end"
                style="rotate: -45deg"
            ></pc-icon>
        </pc-button>
    </pc-tooltip>
</div>

<style>
    .tooltip-placement {
        display: grid;
        grid-template-columns: repeat(5, 1fr);
        grid-template-rows: repeat(5, auto);
        grid-template-areas:
            ".          top-start    top    top-end    .          "
            "left-start .            .      .          right-start"
            "left       .            .      .          right      "
            "left-end   .            .      .          right-end  "
            ".          bottom-start bottom bottom-end .          ";
        gap: var(--pc-spacing-xxs);
        margin: 1rem;
        inline-size: fit-content;
    }

    .tooltip-placement pc-tooltip {
        display: block;
    }

    .tooltip-placement pc-tooltip pc-button::part(base) {
        aspect-ratio: 1 / 1;
    }
</style>
```

### Click trigger

Set the `trigger` attribute to `click` to toggle the tooltip on click instead of hover.

```demo
<pc-tooltip content="Click again to dismiss" trigger="click">
    <pc-button>Click to toggle</pc-button>
</pc-tooltip>
```

### Manual trigger

Tooltips can be controlled programmatically by setting the `trigger` attribute to `manual`. Use the `open` attribute to control when the tooltip is shown.

```demo
<pc-button style="margin-inline-end: 4rem">Toggle manually</pc-button>

<pc-tooltip class="tooltip-manual" content="This is an avatar" trigger="manual">
    <pc-avatar label="User"></pc-avatar>
</pc-tooltip>

<script>
    const tooltip = document.querySelector(".tooltip-manual");
    const toggle = tooltip.previousElementSibling;

    toggle.addEventListener("click", () => (tooltip.open = !tooltip.open));
</script>
```

### Removing arrows

You can control the size of tooltip arrows by overriding the `--pc-tooltip-arrow-size` custom property. To remove them, set the value to `0` as shown below.

```demo
<pc-tooltip
    content="This is a tooltip without an arrow."
    style="--pc-tooltip-arrow-size: 0"
>
    <pc-button>No arrow</pc-button>
</pc-tooltip>
```

To override it globally, set it in a root block in your styles after the Placer Toolkit styles are loaded.

```css
:root {
    --pc-tooltip-arrow-size: 0;
}
```

### HTML in tooltips

Use the `content` slot to create tooltips with HTML content. Tooltips are designed only for text and presentational elements. Avoid placing interactive content, such as buttons, links and form controls, in a tooltip.

```demo
<pc-tooltip>
    <div slot="content">
        I’m not <strong>just</strong> a tooltip, I’m a <em>tooltip</em> with HTML!
    </div>

    <pc-button>Hover me</pc-button>
</pc-tooltip>
```

### Set a maximum width

Use the `--max-width` custom property to change the width the tooltip can grow to before it wraps.

```demo
<pc-tooltip
    content="This tooltip will wrap after 80 pixels."
    style="--max-width: 80px"
>
    <pc-button>Hover me</pc-button>
</pc-tooltip>
```
