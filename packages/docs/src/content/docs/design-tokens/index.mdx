---
title: Design tokens
description: A theme is a collection of predefined CSS custom properties that control global styles from colour to shadows. These custom properties thread through all Placer Toolkit components for a consistent look and feel.
---

import DesignTokens from "../../../components/catalog/DesignTokens.astro";

A theme is a collection of predefined CSS custom properties that control global styles from colour to shadows. These custom properties thread through all Placer Toolkit components for a consistent look and feel.

<DesignTokens />
